{
  "name": "atlastime",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "dev:mobile": "vite --host 0.0.0.0",
    "preview:mobile": "vite preview --host 0.0.0.0 --configLoader runner",
    "check:server": "node --check server/googleCalendarGateway.mjs && node --check server/index.mjs",
    "build": "npm run check:server && tsc --noEmit -p tsconfig.app.json && vite build --configLoader runner",
    "preview": "vite preview",
    "preview:connected": "npm run build && node server/index.mjs",
    "start": "node server/index.mjs",
    "test": "vitest run --configLoader runner"
  },
  "dependencies": {
    "@vitejs/plugin-react": "^4.3.4",
    "lucide-react": "^0.468.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "typescript": "~5.7.2",
    "vite": "^6.0.5"
  },
  "devDependencies": {
    "@types/react": "^19.0.2",
    "@types/react-dom": "^19.0.2",
    "jsdom": "^29.1.1",
    "vitest": "^4.1.10"
  }
}
